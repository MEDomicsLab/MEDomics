---
phase: 02-text-onboarding-implementation-hardening
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - test/extractionTransformerText/create_small_subset.py
  - test/extractionTransformerText/test_transformer_text_extraction.py
  - test/extractionTransformerText/test_transformer_text_extraction_models.py
  - pythonCode/tests/extraction_text/test_biobert_extraction.py
autonomous: true
requirements: [R5, N3]
must_haves:
  truths:
    - "Phase can run baseline regression assets for TransformerText onboarding"
    - "Legacy BioBERT validation script still operates as regression guard"
    - "Onboarded model has an explicit acceptance command in repository"
  artifacts:
    - path: "test/extractionTransformerText/test_transformer_text_extraction_models.py"
      provides: "Model onboarding regression script"
      contains: "scibert_scivocab_uncased"
    - path: "test/extractionTransformerText/test_transformer_text_extraction.py"
      provides: "Transformer extraction contract check"
      contains: "collection_length"
    - path: "test/extractionTransformerText/create_small_subset.py"
      provides: "Small deterministic subset generator"
      contains: "argparse"
  key_links:
    - from: "test/extractionTransformerText/test_transformer_text_extraction_models.py"
      to: "pythonCode/modules/extraction_text/text_model_registry.json"
      via: "model_id list for execution"
      pattern: "text_model_registry.json"
---

<objective>
Restore and align regression assets for onboarding validation and performance-aware checks.

Purpose: make phase exit criteria executable using baseline scripts/tests expected by project constraints.
Output: runnable `test/extractionTransformerText` command set plus aligned BioBERT validation script behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/config.json
@pythonCode/tests/extraction_text/test_biobert_extraction.py
@pythonCode/modules/extraction_text/text_model_registry.json
@pythonCode/modules/extraction_text/text_feature_extraction.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Recreate TransformerText regression script set with onboarding-model coverage</name>
  <files>test/extractionTransformerText/create_small_subset.py, test/extractionTransformerText/test_transformer_text_extraction.py, test/extractionTransformerText/test_transformer_text_extraction_models.py</files>
  <read_first>
    - .planning/config.json
    - pythonCode/tests/extraction_text/test_biobert_extraction.py
    - pythonCode/modules/extraction_text/text_feature_extraction.py
  </read_first>
  <behavior>
    - Test 1: subset generator produces deterministic output CSV with required text/id columns.
    - Test 2: transformer extraction contract script validates output includes prefixed embedding columns and reports row count.
    - Test 3: model sweep script includes and reports the newly onboarded `scibert_scivocab_uncased` model.
  </behavior>
  <action>
    Create `test/extractionTransformerText/` directory and add three scripts aligned to current extraction flow:
    - `create_small_subset.py`: CLI script taking input/output CSV paths + row count; writes deterministic subset preserving `subject_id` and notes column.
    - `test_transformer_text_extraction.py`: CLI validation script that calls transformer extraction helper path and validates output schema contains `collection_length`-equivalent summary and embedding-prefixed columns.
    - `test_transformer_text_extraction_models.py`: CLI model sweep script iterating registry model IDs (including `scibert_scivocab_uncased`), logging success/fail per model, and exiting non-zero when no model passes.

    Keep scripts lightweight and executable in isolation, using argparse and explicit exit codes.

  </action>
  <acceptance_criteria>
    - `test/extractionTransformerText/create_small_subset.py` contains `if __name__ == "__main__":`
    - `test/extractionTransformerText/test_transformer_text_extraction_models.py` contains `scibert_scivocab_uncased`
    - `test/extractionTransformerText/test_transformer_text_extraction.py` contains `embedding`
  </acceptance_criteria>
  <verify>
    <automated>python test/extractionTransformerText/test_transformer_text_extraction_models.py --help</automated>
  </verify>
  <done>Baseline TransformerText regression scripts exist and include explicit onboarding-model coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Align legacy BioBERT validation script with hardening contract checks</name>
  <files>pythonCode/tests/extraction_text/test_biobert_extraction.py</files>
  <read_first>
    - pythonCode/tests/extraction_text/test_biobert_extraction.py
    - pythonCode/modules/extraction_text/BioBERT_extraction.py
    - test/extractionTransformerText/test_transformer_text_extraction.py
  </read_first>
  <action>
    Update `test_biobert_extraction.py` to keep current CLI behavior while adding explicit contract diagnostics used in phase hardening:
    - print selected `column_prefix`, embedding dimension, processed row count, and missing/empty note count summary;
    - ensure non-zero exit on missing required columns or model load errors (already present, preserve);
    - keep invocation compatible with existing command in `.planning/PHASE_1.md` regression matrix.
  </action>
  <acceptance_criteria>
    - `pythonCode/tests/extraction_text/test_biobert_extraction.py` contains `Embedding dimension:`
    - `pythonCode/tests/extraction_text/test_biobert_extraction.py` contains `missing`
    - script still accepts positional args `biobert_path`, `input_csv`, `output_csv`
  </acceptance_criteria>
  <verify>
    <automated>python pythonCode/tests/extraction_text/test_biobert_extraction.py --help</automated>
  </verify>
  <done>Legacy BioBERT validation remains usable and now reports contract diagnostics needed for rollout hardening.</done>
</task>

</tasks>

<verification>
- Run: `python test/extractionTransformerText/test_transformer_text_extraction_models.py --help`
- Run: `python pythonCode/tests/extraction_text/test_biobert_extraction.py --help`
</verification>

<success_criteria>

- `test/extractionTransformerText/` baseline scripts exist and are runnable.
- Onboarded model is represented in validation scripts.
- Legacy BioBERT validation script remains compatible and diagnostics are improved.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-text-onboarding-implementation-hardening/02-03-SUMMARY.md`
</output>
