---
phase: 02-text-onboarding-implementation-hardening
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - pythonCode/modules/extraction_text/text_feature_extraction.py
  - go_server/blueprints/extraction_text/extraction_text.go
  - pythonCode/tests/extraction_text/test_text_dispatch_fallback.py
autonomous: true
requirements: [R3, R4, N1, N2]
must_haves:
  truths:
    - "Existing extraction routes continue to respond on unchanged paths"
    - "TransformerText requests resolve handlers through registry-backed dispatch"
    - "Model load failures in gated mode fallback to stable default path"
  artifacts:
    - path: "pythonCode/modules/extraction_text/text_feature_extraction.py"
      provides: "Registry-backed model resolution + fallback"
      contains: "resolve_model_path"
    - path: "pythonCode/tests/extraction_text/test_text_dispatch_fallback.py"
      provides: "Dispatch and fallback behavioral verification"
      contains: "test_fallback_to_default_predefined_model"
    - path: "go_server/blueprints/extraction_text/extraction_text.go"
      provides: "Unchanged public extraction routes"
      contains: "TransformerText_extraction"
  key_links:
    - from: "renderer/components/extractionTabular/extractionTabularData.jsx"
      to: "go_server/blueprints/extraction_text/extraction_text.go"
      via: "serverUrl + extractionFunction"
      pattern: "TransformerText_extraction"
    - from: "go_server/blueprints/extraction_text/extraction_text.go"
      to: "pythonCode/modules/extraction_text/text_feature_extraction.py"
      via: "StartPythonScripts"
      pattern: "text_feature_extraction.py"
---

<objective>
Implement route-stable backend dispatch hardening with model fallback controls.

Purpose: enable safe onboarding growth without breaking existing extraction endpoints or legacy behavior.
Output: fallback-capable TransformerText resolution path + dispatch tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-text-onboarding-implementation-hardening/02-01-PLAN.md
@pythonCode/modules/extraction_text/text_feature_extraction.py
@go_server/blueprints/extraction_text/extraction_text.go

<interfaces>
From `go_server/blueprints/extraction_text/extraction_text.go`:
```go
Utils.CreateHandleFunc(prePath+"/BioBERT_extraction/", handleBioBERTExtraction)
Utils.CreateHandleFunc(prePath+"/TransformerText_extraction/", handleTransformerExtraction)
```

From `renderer/components/extractionTabular/extractionTabularData.jsx`:

```javascript
requestBackend(port, serverUrl + extractionFunction + "/" + pageId, {
  relativeToExtractionType: extractionJsonData,
  identifiersList: subList,
  ...
})
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add registry-backed model resolver and fallback gate in TransformerText extraction</name>
  <files>pythonCode/modules/extraction_text/text_feature_extraction.py, pythonCode/tests/extraction_text/test_text_dispatch_fallback.py</files>
  <read_first>
    - pythonCode/modules/extraction_text/text_feature_extraction.py
    - pythonCode/modules/extraction_text/text_model_registry.json
    - pythonCode/modules/extraction_text/BioBERT_extraction.py
  </read_first>
  <behavior>
    - Test 1: predefined `model_id` resolves to corresponding `hf_model_name_or_path` from registry.
    - Test 2: unknown `model_id` raises deterministic `ValueError` listing accepted IDs when fallback disabled.
    - Test 3: model load failure falls back to default predefined model when `allow_model_fallback=true` in extraction config.
  </behavior>
  <action>
    In `text_feature_extraction.py`, replace direct `PREDEFINED_MODELS` lookup path with helper functions:
    - `load_text_model_registry()` reads `text_model_registry.json`.
    - `get_default_model_id(registry)` returns model where `default_for_transformer_text=true`.
    - `resolve_model_path(model_source_type, model_name_or_path, registry)` handles predefined/local resolution.

    Add config key handling in `relativeToExtractionType`:
    - `allow_model_fallback` (default `true` for backward compatibility).
    During model load, if primary model fails and `allow_model_fallback=true`, retry using default model path from registry; if fallback also fails, raise RuntimeError including both attempted model paths.

    Add `test_text_dispatch_fallback.py` using pytest + monkeypatch to mock `AutoTokenizer.from_pretrained` and `AutoModel.from_pretrained` behaviors for fail-then-pass fallback flow.

  </action>
  <acceptance_criteria>
    - `pythonCode/modules/extraction_text/text_feature_extraction.py` contains `def resolve_model_path(`
    - `pythonCode/modules/extraction_text/text_feature_extraction.py` contains `allow_model_fallback`
    - `pythonCode/tests/extraction_text/test_text_dispatch_fallback.py` contains `def test_fallback_to_default_predefined_model(`
  </acceptance_criteria>
  <verify>
    <automated>pytest pythonCode/tests/extraction_text/test_text_dispatch_fallback.py -q</automated>
  </verify>
  <done>TransformerText uses registry-backed resolution with controlled fallback behavior and deterministic failure semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve and assert Go route compatibility surface</name>
  <files>go_server/blueprints/extraction_text/extraction_text.go, pythonCode/tests/extraction_text/test_text_dispatch_fallback.py</files>
  <read_first>
    - go_server/blueprints/extraction_text/extraction_text.go
    - renderer/components/extractionTabular/extractionTabularData.jsx
  </read_first>
  <action>
    Keep `CreateHandleFunc` route registrations exactly unchanged for `BioBERT_extraction`, `TransformerText_extraction`, and `progress`. If adding logging for diagnosis, only add non-breaking log statements and do not rename handler funcs or route strings.

    Extend `test_text_dispatch_fallback.py` with a static contract assertion that reads `extraction_text.go` and verifies all three literal route suffixes are present; this prevents accidental path drift during hardening.

  </action>
  <acceptance_criteria>
    - `go_server/blueprints/extraction_text/extraction_text.go` still contains `/BioBERT_extraction/`
    - `go_server/blueprints/extraction_text/extraction_text.go` still contains `/TransformerText_extraction/`
    - `pythonCode/tests/extraction_text/test_text_dispatch_fallback.py` contains `test_go_routes_remain_stable`
  </acceptance_criteria>
  <verify>
    <automated>pytest pythonCode/tests/extraction_text/test_text_dispatch_fallback.py -q</automated>
  </verify>
  <done>Go endpoint contracts are unchanged and protected by an automated regression assertion.</done>
</task>

</tasks>

<verification>
- Run: `pytest pythonCode/tests/extraction_text/test_text_dispatch_fallback.py -q`
- Confirm route strings in Go extraction blueprint remain unchanged.
</verification>

<success_criteria>

- TransformerText model resolution and fallback are implemented without route contract changes.
- Automated tests cover resolver behavior, fallback behavior, and route stability.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-text-onboarding-implementation-hardening/02-02-SUMMARY.md`
</output>
