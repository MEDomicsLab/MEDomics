---
phase: 02-text-onboarding-implementation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pythonCode/modules/extraction_text/text_model_registry.json
  - renderer/components/extractionTabular/extractionTypes/textModelRegistry.js
  - renderer/components/extractionTabular/extractionTypes/extractionTransformerText.jsx
  - pythonCode/tests/extraction_text/test_text_model_registry.py
autonomous: true
requirements: [R1, R2, N4]
must_haves:
  truths:
    - "TransformerText model options are defined from one canonical registry contract"
    - "Renderer-visible model IDs remain parity-aligned with backend registry"
    - "Existing TransformerText defaults still produce a valid request payload"
  artifacts:
    - path: "pythonCode/modules/extraction_text/text_model_registry.json"
      provides: "Canonical text model contract and onboarding metadata"
      contains: "model_id"
    - path: "renderer/components/extractionTabular/extractionTypes/textModelRegistry.js"
      provides: "Renderer-consumable registry projection"
      exports: ["TEXT_MODEL_REGISTRY", "TEXT_MODEL_DETAILS"]
    - path: "pythonCode/tests/extraction_text/test_text_model_registry.py"
      provides: "Registry schema and parity validation"
      contains: "test_renderer_registry_ids_match_backend_registry"
  key_links:
    - from: "pythonCode/modules/extraction_text/text_model_registry.json"
      to: "renderer/components/extractionTabular/extractionTypes/textModelRegistry.js"
      via: "shared model_id set"
      pattern: "model_id"
    - from: "renderer/components/extractionTabular/extractionTypes/extractionTransformerText.jsx"
      to: "textModelRegistry.js"
      via: "imported registry constants"
      pattern: "from \"\\./textModelRegistry\""
---

<objective>
Create the canonical text model registry and wire renderer model selection to that contract.

Purpose: remove UI/backend model drift risk while preserving current TransformerText behavior.
Output: registry artifact, renderer registry projection, and parity tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-text-onboarding-implementation-hardening/02-RESEARCH.md
@pythonCode/modules/extraction_text/text_feature_extraction.py
@renderer/components/extractionTabular/extractionTypes/extractionTransformerText.jsx

<interfaces>
From `pythonCode/modules/extraction_text/text_feature_extraction.py`:
```python
PREDEFINED_MODELS = {
    "biobert_v1_1": "dmis-lab/biobert-v1.1",
    ...
}
```

From `renderer/components/extractionTabular/extractionTypes/extractionTransformerText.jsx`:

```javascript
const config = {
  selectedColumns,
  columnPrefix,
  model_source_type: modelSourceType,
  model_name_or_path: modelSourceType === "predefined" ? selectedModel : localModelPath,
  aggregation_mode: aggregationMode,
  frequency: aggregationMode === "note" ? "Note" : "Patient"
}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical text model registry contract</name>
  <files>pythonCode/modules/extraction_text/text_model_registry.json, pythonCode/tests/extraction_text/test_text_model_registry.py</files>
  <read_first>
    - pythonCode/modules/extraction_text/text_feature_extraction.py
    - pythonCode/modules/extraction_text/selected_models.txt
    - pythonCode/tests/extraction_text/test_biobert_extraction.py
  </read_first>
  <behavior>
    - Test 1: every registry entry has required keys (`model_id`, `label`, `hf_model_name_or_path`, `source_type`, `hidden_size`, `enabled`, `default_for_transformer_text`, `validation_status`).
    - Test 2: exactly one entry has `default_for_transformer_text=true` and it is `biobert_v1_1` to preserve legacy default behavior.
    - Test 3: registry includes at least one newly onboarded model ID not present in current `PREDEFINED_MODELS` (use `scibert_scivocab_uncased`).
  </behavior>
  <action>
    Create `text_model_registry.json` with a top-level `version` string and `models` array. Include all currently supported TransformerText model IDs and add `scibert_scivocab_uncased` (`allenai/scibert_scivocab_uncased`) as the new onboarded model (additive, per state decision to expand model onboarding while preserving compatibility). Set `enabled=true` for legacy models and the new model, set `default_for_transformer_text=true` only on `biobert_v1_1`, and set `validation_status` values (`legacy-verified` for existing, `candidate` for the new one).

    Implement `test_text_model_registry.py` to load JSON via `json` module, assert schema keys for every model, assert unique `model_id`, assert one default model, and assert presence of `scibert_scivocab_uncased`.

  </action>
  <acceptance_criteria>
    - `pythonCode/modules/extraction_text/text_model_registry.json` contains `"model_id": "scibert_scivocab_uncased"`
    - `pythonCode/modules/extraction_text/text_model_registry.json` contains exactly one `"default_for_transformer_text": true`
    - `pythonCode/tests/extraction_text/test_text_model_registry.py` contains `def test_registry_contains_new_onboarded_model(`
  </acceptance_criteria>
  <verify>
    <automated>pytest pythonCode/tests/extraction_text/test_text_model_registry.py -q</automated>
  </verify>
  <done>Registry contract exists, validates structurally, and includes one newly onboarded text model without changing legacy default selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire renderer model picker to registry projection with parity check</name>
  <files>renderer/components/extractionTabular/extractionTypes/textModelRegistry.js, renderer/components/extractionTabular/extractionTypes/extractionTransformerText.jsx, pythonCode/tests/extraction_text/test_text_model_registry.py</files>
  <read_first>
    - renderer/components/extractionTabular/extractionTypes/extractionTransformerText.jsx
    - renderer/components/extractionTabular/extractionTabularData.jsx
    - pythonCode/modules/extraction_text/text_model_registry.json
  </read_first>
  <behavior>
    - Test 1: renderer dropdown options are derived from imported registry constants (not inline hardcoded arrays).
    - Test 2: parity test fails if backend `model_id` set diverges from renderer exported IDs.
    - Test 3: generated extraction config still includes `model_source_type`, `model_name_or_path`, and `frequency` keys.
  </behavior>
  <action>
    Create `textModelRegistry.js` exporting `TEXT_MODEL_REGISTRY` and `TEXT_MODEL_DETAILS` populated from the canonical JSON values (copied as explicit constants in this file for renderer import safety). Update `extractionTransformerText.jsx` to remove in-file `PREDEFINED_MODELS` and `MODEL_DETAILS` literals, import from `./textModelRegistry`, and keep existing request payload keys unchanged to preserve contract compatibility (per STATE compatibility priority).

    Extend `test_text_model_registry.py` with `test_renderer_registry_ids_match_backend_registry` that loads backend JSON and parses `textModelRegistry.js` for `value:` entries using regex, asserting exact set equality.

  </action>
  <acceptance_criteria>
    - `renderer/components/extractionTabular/extractionTypes/extractionTransformerText.jsx` contains `from "./textModelRegistry"`
    - `renderer/components/extractionTabular/extractionTypes/extractionTransformerText.jsx` no longer contains `const PREDEFINED_MODELS = [`
    - `pythonCode/tests/extraction_text/test_text_model_registry.py` contains `test_renderer_registry_ids_match_backend_registry`
  </acceptance_criteria>
  <verify>
    <automated>pytest pythonCode/tests/extraction_text/test_text_model_registry.py -q</automated>
  </verify>
  <done>Renderer model picker uses registry projection and automated parity checks detect drift between backend/renderer model IDs.</done>
</task>

</tasks>

<verification>
- Run: `pytest pythonCode/tests/extraction_text/test_text_model_registry.py -q`
- Confirm no change to request envelope keys in `ExtractionTransformerText` config payload.
</verification>

<success_criteria>

- Canonical registry file exists and is test-validated.
- Renderer TransformerText model options are sourced from registry projection.
- At least one newly onboarded model (`scibert_scivocab_uncased`) is available in UI selection list.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-text-onboarding-implementation-hardening/02-01-SUMMARY.md`
</output>
